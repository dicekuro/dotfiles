#------------------------------------------------------------------------------
# プロファイル関連
#   zsh起動時にプロファイリングする時は、以下の行のコメントを外す
#   なお、"zmodload zsh/zprof && zprof"は".zshenv"の最初に記載すること
#------------------------------------------------------------------------------
#szmodload zsh/zprof && zprof
